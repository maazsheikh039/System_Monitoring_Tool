System Monitoring Tool

Built a terminal-based Linux system resource monitor in Python, parsing kernel data directly from the /proc filesystem (/proc/stat, /proc/meminfo) to compute real-time CPU and memory utilization, with live-refreshing terminal output.
